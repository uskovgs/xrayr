# Example
# .onLoad <- function(libname, pkgname) {
#   op <- options()
#   op.xrayr <- list(
#     xrayr.show = TRUE
#   )
#   toset <- !(names(op.xrayr) %in% names(op))
#   if (any(toset)) options(op.xrayr[toset])
#
#   invisible()
# }


